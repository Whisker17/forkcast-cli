{
  "name": "forkcast-cli",
  "version": "0.1.0",
  "description": "CLI for querying Ethereum governance data from forkcast",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "forkcast": "bin/forkcast"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc --watch -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "node --test test/whi-56.test.mjs"
  },
  "keywords": [
    "cli",
    "ethereum",
    "forkcast"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5.9.3"
  }
}
